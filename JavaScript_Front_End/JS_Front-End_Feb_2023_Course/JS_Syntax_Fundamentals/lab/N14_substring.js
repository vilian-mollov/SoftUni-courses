let text = 'ASentence';
let start = 1;
let end = 8;

function substringFormText (text, start, end) {
    return text.substring(start, start + end);
}


console.log(substringFormText(text, start, end));
